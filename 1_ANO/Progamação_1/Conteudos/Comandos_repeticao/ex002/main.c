#include <stdio.h>

int main() {

    int x, y

    printf("Digite o valor de x: ");
    scanf("%d", &x);

    printf("Digite o valor de y: ");
    scanf("Digite o valor de y: ");

    for (int i = x + 1; i < y; 1++) {

        if(1 % 5 == 2 || i % 5 == 3){
        printf("%d", i);
        }
    }

    return 0;
}